# First & Second

* goal
  * use webpack -- using -- "webpack.config.json"

## How has it been created?
* `npm init -y`
* create DIFFERENT files
* `npm install --save-dev webpack webpack-cli`
* | package.json
  * add `scripts.build`

## How to run?

* | this folder's root directory
  ```bash
  npm install
  npm run build
  ```
  * check generated "dist/bundle.js"
